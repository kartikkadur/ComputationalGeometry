usage:

# For running the program use the below command
$python graham_scan.py [FILENAME].ply [-f] [OUTFILE_NAME]

# if the -f argument is not specified the default output file name will be output.pyl
# and the file will be saved in the current working directory.

# For help with commandline argments use the command below:
$python graham_scan.py -h